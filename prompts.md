# プロンプトログ

## 2026-09-15

> この要件定義所を、状態遷移表・業務ルール・表示ルール・メッセージ一覧を実装単位に分解して

> そんな感じで、社内問い合わせ管理システム/ステータス変更・担当者割り当て機能一旦実装して。問い合わせの登録・一覧表示機能は実装済みという想定です、今回はステータス変更と担当者割り当てのみを実装して。

> 実装まででいいよ、モック画像とかは自分でスクショする

> 最初未対応の状態で、対応中を選べるようにして。また、保留の時、完了を選べるようにして、選んだらエラーメッセージ表示して。

> 未対応から完了も選べて、同じようにエラー出るようにして。また、R-01〜R-06に則り、担当者割り当てルールと、権限による操作の出し分け機能追加して。

> V-01〜V-07の表示ルールを追加実装。PENDINGでは完了を選べないようにし、最後はMEMBERとADMINの2つのURLを用意する。

> ステータス変更に伴う付随処理を追加実装。NEW → IN_PROGRESSの自動担当者設定、DONE到達時のclosed_at設定、DONE → IN_PROGRESSのclosed_atクリア、全遷移の履歴登録とupdated_at更新。

> 2.8の処理結果メッセージ9種類を、句読点まで完全一致で追加実装する。

> リセットボタンを作り、押すと全ての権限で初期化されるようにする。


> セルフチェックリスト13項目は自分で行うため、AI側では実施しなくてよい。

> リポジトリ名は「NEXUS-Gate1」とする。

> 作ったものを検証のために開く。

> 権限の切り替えは一般的にどう行うか。

> READMEを指定された6章構成（起動手順、動作確認手順、実装できなかった箇所、セルフチェック結果、モック画像一覧、振り返り）で書き直す。

> GitHubのnexus-gate1リポジトリに変更が反映されているか確認する。

> READMEに本来記載する起動コマンドを確認する。

> MEMBERとADMINの2つのURLで表示する。

> `closed_at`がどこに表示されるか、どのように確認できるか確認する。

> GitHub上では`closed_at`に関する処理がどのファイルにあるか確認する。

> データを初期状態に戻し、MEMBERとADMINの2つのURLを提示する。

> 画面操作で値を変更した場合、GitHub上のDBもリアルタイムに変更されるか確認する。

> 実行中のデータをリアルタイムに確認する方法を確認する。

> URLを閉じるたびに初期化する案を検討したが、実装は現状のままとする。

> 「履歴登録を意図的に失敗させると、本体の更新もロールバックされる」の意味と確認方法を確認する。

> 実行中のデータを何で管理しているか、簡単に確認する方法を確認する。

> 履歴登録を意図的に失敗させる方法を確認する。

> 履歴登録失敗時に本体更新もロールバックされることを実際にチェックする。

> GitHubのREADMEについて、起動手順・動作確認手順・実装できなかった箇所を正確に書き直し、リセットボタンは要件外だがテスト用に実装したことを明記する。

> 最終確認用のMEMBERとADMINの2つのURLを提示する。

> READMEに記載した環境構築・起動コマンドを実行しないと2つのURLを開けないのか確認する。

> 環境構築と起動を行えば誰でも同じURLを開けるのか確認する。

> 2つのURLを開けるよう、GitHubのREADMEに起動手順が正しく書かれているか確認して修正する。

> 自動テストのコードと実行方法を確認する。

> 自動テストの目的と、実装過程で作成したものか確認する。


> 課題提出用のGitHubリポジトリに不要なものがないか確認し、削除前に許可を求める。

> ここまでのコード変更がコミット・プッシュ済みか確認し、未反映なら実施する。

> プロンプトログを最新の内容へ更新する。

